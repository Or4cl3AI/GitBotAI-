from setuptools import setup, find_packages

setup(
    name="Sweep",
    version="1.0.0",
    author="Your Name",
    author_email="your.email@example.com",
    description="Sweep AI turns bug fixes & feature requests into code changes",
    url="https://sweep.dev",
    packages=find_packages(),
    install_requires=[
        "dependency1",
        "dependency2",
        # Add more dependencies as needed
    ],
)